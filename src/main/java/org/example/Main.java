package org.example;

import org.example.setting.Startedconf;

public class Main
{
    public static void main(String[] arguments) throws Exception
    {
         Startedconf startedconf =new Startedconf();
         startedconf.fonksiyon();

 
    }
}
